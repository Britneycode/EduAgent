# JWT 认证系统 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 为 EduAgent 添加用户名密码注册/登录 + JWT 认证，保护所有 API 端点，前端适配登录流程。

**Architecture:** 新增 `users` 表和 `User` 模型，`core/auth.py` 提供密码哈希和 JWT 工具函数及 `get_current_user` FastAPI 依赖。注册/登录端点在 `api/auth.py`。现有 chat/profile 端点注入当前用户，`chat_sessions` 新增 `user_id` 列实现数据隔离。前端新增 `lib/auth.ts` token 管理、登录页面、请求拦截器和路由保护。

**Tech Stack:** PyJWT, passlib[bcrypt], FastAPI Depends, Next.js App Router, localStorage

---

## 文件结构

| 操作 | 文件 | 职责 |
|------|------|------|
| 修改 | `backend/pyproject.toml` | 添加 pyjwt、passlib 依赖 |
| 修改 | `backend/app/core/config.py` | 添加 JWT 配置字段 |
| 创建 | `backend/app/models/user.py` | User SQLAlchemy 模型 |
| 修改 | `backend/app/core/database.py:init_db` | 导入 user 模型 |
| 创建 | `backend/app/core/auth.py` | 密码哈希、JWT 创建/验证、get_current_user |
| 创建 | `backend/app/schemas/auth.py` | 注册/登录请求响应 schema |
| 创建 | `backend/app/api/auth.py` | 注册/登录路由 |
| 修改 | `backend/app/main.py` | 注册 auth_router |
| 修改 | `backend/app/models/chat.py` | ChatSession 添加 user_id 列 |
| 修改 | `backend/app/services/chat_service.py` | 所有查询过滤 user_id |
| 修改 | `backend/app/api/chat.py` | 注入 get_current_user |
| 修改 | `backend/app/services/profile_service.py` | 移除 DEFAULT_USER_ID |
| 修改 | `backend/app/api/profile.py` | 注入 get_current_user |
| 修改 | `backend/app/api/wiki.py` | write-back 端点注入认证 |
| 创建 | `frontend/src/lib/auth.ts` | Token 存取、过期检查 |
| 修改 | `frontend/src/lib/api.ts` | 请求头注入 Authorization |
| 修改 | `frontend/src/lib/sse.ts` | SSE 请求头注入 Authorization |
| 创建 | `frontend/src/app/login/page.tsx` | 登录/注册页面 |
| 修改 | `frontend/src/app/(main)/layout.tsx` | 路由保护 |
| 修改 | `frontend/src/app/page.tsx` | 未登录重定向到 /login |
| 创建 | `backend/tests/test_auth.py` | 认证相关测试 |

---

### Task 1: 添加依赖 + JWT 配置

**Files:**
- Modify: `backend/pyproject.toml:6-17`
- Modify: `backend/app/core/config.py:6-18`

- [ ] **Step 1: 添加 Python 依赖**

在 `backend/pyproject.toml` 的 `dependencies` 列表末尾添加：

```toml
dependencies = [
    "fastapi>=0.115,<1.0",
    "httpx>=0.27,<1.0",
    "pydantic-settings>=2.3,<3.0",
    "sqlalchemy[asyncio]>=2.0.0",
    "uvicorn>=0.30,<1.0",
    "numpy>=1.26,<3.0",
    "sentence-transformers>=3.0,<4.0",
    "asyncpg>=0.30.0",
    "aiosqlite>=0.21.0",
    "alembic>=1.15.0",
    "pyjwt>=2.9.0",
    "passlib[bcrypt]>=1.7.4",
]
```

- [ ] **Step 2: 添加 JWT 配置字段**

在 `backend/app/core/config.py` 的 `Settings` 类中，在 `spark_dev_mode` 之后添加：

```python
class Settings(BaseSettings):
    """应用基础配置。"""

    app_name: str = "EduAgent API"
    app_version: str = "0.1.0"
    database_url: str = "sqlite+aiosqlite:///./eduagent.db"
    backend_cors_origins: list[str] = ["http://localhost:3000"]
    spark_app_id: str = ""
    spark_api_key: str = ""
    spark_api_secret: str = ""
    spark_dev_mode: bool = False

    # JWT 认证
    jwt_secret_key: str = "dev-secret-change-in-production"
    jwt_algorithm: str = "HS256"
    jwt_expire_minutes: int = 1440

    # Wiki 知识中枢配置
    wiki_chroma_dir: str = "./chroma_data"
    wiki_knowledge_dir: str = "./knowledge/ai_intro"
    wiki_embedding_dev_mode: bool = False
    wiki_auto_ingest: bool = True

    model_config = SettingsConfigDict(
        env_file=".env",
        env_file_encoding="utf-8",
        case_sensitive=False,
        extra="ignore",
    )
```

- [ ] **Step 3: 安装依赖**

Run: `cd backend && uv sync`
Expected: 依赖安装成功，无报错

- [ ] **Step 4: 验证配置加载**

Run: `cd backend && uv run python -c "from app.core.config import get_settings; s = get_settings(); print(s.jwt_secret_key, s.jwt_algorithm, s.jwt_expire_minutes)"`
Expected: `dev-secret-change-in-production HS256 1440`

- [ ] **Step 5: 提交**

```bash
git add backend/pyproject.toml backend/app/core/config.py backend/uv.lock
git commit -m "feat: 添加 JWT 认证依赖和配置"
```

---

### Task 2: User 模型 + Alembic 迁移

**Files:**
- Create: `backend/app/models/user.py`
- Modify: `backend/app/core/database.py:37-41`
- Test: `backend/tests/test_auth.py`

- [ ] **Step 1: 编写 User 模型测试**

创建 `backend/tests/test_auth.py`：

```python
from __future__ import annotations

import pytest
from sqlalchemy import select
from sqlalchemy.ext.asyncio import AsyncSession

from app.models.user import User


@pytest.mark.asyncio
async def test_create_user(async_session: AsyncSession) -> None:
    user = User(username="testuser", hashed_password="fakehash", display_name="测试用户")
    async_session.add(user)
    await async_session.commit()
    await async_session.refresh(user)

    assert user.id is not None
    assert user.username == "testuser"
    assert user.display_name == "测试用户"


@pytest.mark.asyncio
async def test_username_unique(async_session: AsyncSession) -> None:
    user1 = User(username="dup", hashed_password="hash1")
    async_session.add(user1)
    await async_session.commit()

    user2 = User(username="dup", hashed_password="hash2")
    async_session.add(user2)
    with pytest.raises(Exception):
        await async_session.commit()
```

- [ ] **Step 2: 运行测试确认失败**

Run: `cd backend && uv run pytest tests/test_auth.py -v`
Expected: FAIL — `ModuleNotFoundError: No module named 'app.models.user'`

- [ ] **Step 3: 创建 User 模型**

创建 `backend/app/models/user.py`：

```python
from __future__ import annotations

from datetime import UTC, datetime

from sqlalchemy import DateTime, String
from sqlalchemy.orm import Mapped, mapped_column

from app.core.database import Base


class User(Base):
    """用户模型。"""

    __tablename__ = "users"

    id: Mapped[int] = mapped_column(primary_key=True)
    username: Mapped[str] = mapped_column(String(50), unique=True, nullable=False)
    hashed_password: Mapped[str] = mapped_column(String(255), nullable=False)
    display_name: Mapped[str | None] = mapped_column(String(100), nullable=True)
    created_at: Mapped[datetime] = mapped_column(
        DateTime, default=lambda: datetime.now(UTC), nullable=False
    )
```

- [ ] **Step 4: 在 init_db 中注册 User 模型**

修改 `backend/app/core/database.py` 的 `init_db` 函数，添加 user 导入：

```python
async def init_db() -> None:
    """初始化数据库并创建所有已注册的数据表。"""
    from app.models import chat, profile, resource, user, wiki  # noqa: F401

    async with engine.begin() as conn:
        await conn.run_sync(Base.metadata.create_all)
```

- [ ] **Step 5: 运行测试确认通过**

Run: `cd backend && uv run pytest tests/test_auth.py -v`
Expected: 2 passed

- [ ] **Step 6: 生成 Alembic 迁移**

Run: `cd backend && uv run alembic revision --autogenerate -m "添加 users 表"`
Expected: 生成新迁移文件

- [ ] **Step 7: 应用迁移**

Run: `cd backend && uv run alembic upgrade head`
Expected: 迁移成功

- [ ] **Step 8: 提交**

```bash
git add backend/app/models/user.py backend/app/core/database.py backend/tests/test_auth.py backend/alembic/versions/
git commit -m "feat: 添加 User 模型和数据库迁移"
```

---

### Task 3: 认证核心（密码哈希 + JWT + get_current_user）

**Files:**
- Create: `backend/app/core/auth.py`
- Test: `backend/tests/test_auth.py`（追加）

- [ ] **Step 1: 编写密码哈希和 JWT 测试**

在 `backend/tests/test_auth.py` 末尾追加：

```python
from app.core.auth import hash_password, verify_password, create_access_token, decode_access_token


def test_password_hash_and_verify() -> None:
    password = "mypassword123"
    hashed = hash_password(password)
    assert hashed != password
    assert verify_password(password, hashed) is True
    assert verify_password("wrong", hashed) is False


def test_create_and_decode_token() -> None:
    token = create_access_token(user_id=42)
    payload = decode_access_token(token)
    assert payload is not None
    assert payload["sub"] == "42"


def test_decode_invalid_token() -> None:
    payload = decode_access_token("invalid.token.here")
    assert payload is None
```

- [ ] **Step 2: 运行测试确认失败**

Run: `cd backend && uv run pytest tests/test_auth.py::test_password_hash_and_verify tests/test_auth.py::test_create_and_decode_token tests/test_auth.py::test_decode_invalid_token -v`
Expected: FAIL — `ModuleNotFoundError: No module named 'app.core.auth'`

- [ ] **Step 3: 实现 auth.py**

创建 `backend/app/core/auth.py`：

```python
from __future__ import annotations

from datetime import UTC, datetime, timedelta

import jwt
from fastapi import Depends, HTTPException, status
from fastapi.security import HTTPAuthorizationCredentials, HTTPBearer
from passlib.context import CryptContext
from sqlalchemy import select
from sqlalchemy.ext.asyncio import AsyncSession

from app.core.config import get_settings
from app.core.database import get_db
from app.models.user import User

pwd_context = CryptContext(schemes=["bcrypt"], deprecated="auto")
security = HTTPBearer()


def hash_password(password: str) -> str:
    return pwd_context.hash(password)


def verify_password(plain: str, hashed: str) -> bool:
    return pwd_context.verify(plain, hashed)


def create_access_token(user_id: int) -> str:
    settings = get_settings()
    expire = datetime.now(UTC) + timedelta(minutes=settings.jwt_expire_minutes)
    payload = {"sub": str(user_id), "exp": expire}
    return jwt.encode(payload, settings.jwt_secret_key, algorithm=settings.jwt_algorithm)


def decode_access_token(token: str) -> dict | None:
    settings = get_settings()
    try:
        return jwt.decode(token, settings.jwt_secret_key, algorithms=[settings.jwt_algorithm])
    except jwt.PyJWTError:
        return None


async def get_current_user(
    credentials: HTTPAuthorizationCredentials = Depends(security),
    db: AsyncSession = Depends(get_db),
) -> User:
    payload = decode_access_token(credentials.credentials)
    if payload is None:
        raise HTTPException(status_code=status.HTTP_401_UNAUTHORIZED, detail="无效的认证凭据")
    user_id = payload.get("sub")
    if user_id is None:
        raise HTTPException(status_code=status.HTTP_401_UNAUTHORIZED, detail="无效的认证凭据")
    result = await db.execute(select(User).where(User.id == int(user_id)))
    user = result.scalars().first()
    if user is None:
        raise HTTPException(status_code=status.HTTP_401_UNAUTHORIZED, detail="用户不存在")
    return user
```

- [ ] **Step 4: 运行测试确认通过**

Run: `cd backend && uv run pytest tests/test_auth.py -v`
Expected: 5 passed（2 个模型测试 + 3 个 auth 测试）

- [ ] **Step 5: 提交**

```bash
git add backend/app/core/auth.py backend/tests/test_auth.py
git commit -m "feat: 实现密码哈希、JWT 创建/验证和 get_current_user"
```

---

### Task 4: 注册/登录 API 端点

**Files:**
- Create: `backend/app/schemas/auth.py`
- Create: `backend/app/api/auth.py`
- Modify: `backend/app/main.py:8-9,38-39`
- Test: `backend/tests/test_auth.py`（追加）

- [ ] **Step 1: 编写注册/登录 API 测试**

在 `backend/tests/test_auth.py` 末尾追加：

```python
from httpx import ASGITransport, AsyncClient
from app.main import app


@pytest.mark.asyncio
async def test_register_success() -> None:
    transport = ASGITransport(app=app)
    async with AsyncClient(transport=transport, base_url="http://test") as client:
        resp = await client.post("/api/auth/register", json={
            "username": "newuser",
            "password": "password123",
            "display_name": "新用户",
        })
    assert resp.status_code == 200
    data = resp.json()
    assert "access_token" in data
    assert data["token_type"] == "bearer"
    assert data["user_id"] > 0


@pytest.mark.asyncio
async def test_register_duplicate_username() -> None:
    transport = ASGITransport(app=app)
    async with AsyncClient(transport=transport, base_url="http://test") as client:
        await client.post("/api/auth/register", json={
            "username": "dupuser",
            "password": "pass1",
        })
        resp = await client.post("/api/auth/register", json={
            "username": "dupuser",
            "password": "pass2",
        })
    assert resp.status_code == 400


@pytest.mark.asyncio
async def test_login_success() -> None:
    transport = ASGITransport(app=app)
    async with AsyncClient(transport=transport, base_url="http://test") as client:
        await client.post("/api/auth/register", json={
            "username": "loginuser",
            "password": "mypass",
        })
        resp = await client.post("/api/auth/login", json={
            "username": "loginuser",
            "password": "mypass",
        })
    assert resp.status_code == 200
    data = resp.json()
    assert "access_token" in data


@pytest.mark.asyncio
async def test_login_wrong_password() -> None:
    transport = ASGITransport(app=app)
    async with AsyncClient(transport=transport, base_url="http://test") as client:
        await client.post("/api/auth/register", json={
            "username": "wrongpass",
            "password": "correct",
        })
        resp = await client.post("/api/auth/login", json={
            "username": "wrongpass",
            "password": "incorrect",
        })
    assert resp.status_code == 401
```

- [ ] **Step 2: 运行测试确认失败**

Run: `cd backend && uv run pytest tests/test_auth.py::test_register_success -v`
Expected: FAIL — 404（路由不存在）

- [ ] **Step 3: 创建 auth schema**

创建 `backend/app/schemas/auth.py`：

```python
from __future__ import annotations

from pydantic import BaseModel, Field


class RegisterRequest(BaseModel):
    username: str = Field(..., min_length=2, max_length=50, description="用户名")
    password: str = Field(..., min_length=6, max_length=128, description="密码")
    display_name: str | None = Field(default=None, max_length=100, description="显示名称")


class LoginRequest(BaseModel):
    username: str = Field(..., description="用户名")
    password: str = Field(..., description="密码")


class AuthResponse(BaseModel):
    access_token: str
    token_type: str = "bearer"
    user_id: int
```

- [ ] **Step 4: 创建 auth 路由**

创建 `backend/app/api/auth.py`：

```python
from __future__ import annotations

from fastapi import APIRouter, Depends, HTTPException, status
from sqlalchemy import select
from sqlalchemy.ext.asyncio import AsyncSession

from app.core.auth import create_access_token, hash_password, verify_password
from app.core.database import get_db
from app.models.user import User
from app.schemas.auth import AuthResponse, LoginRequest, RegisterRequest

router = APIRouter(prefix="/api/auth", tags=["auth"])


@router.post("/register", response_model=AuthResponse)
async def register(
    request: RegisterRequest,
    db: AsyncSession = Depends(get_db),
) -> AuthResponse:
    result = await db.execute(select(User).where(User.username == request.username))
    if result.scalars().first() is not None:
        raise HTTPException(status_code=status.HTTP_400_BAD_REQUEST, detail="用户名已存在")

    user = User(
        username=request.username,
        hashed_password=hash_password(request.password),
        display_name=request.display_name,
    )
    db.add(user)
    await db.commit()
    await db.refresh(user)

    token = create_access_token(user_id=user.id)
    return AuthResponse(access_token=token, user_id=user.id)


@router.post("/login", response_model=AuthResponse)
async def login(
    request: LoginRequest,
    db: AsyncSession = Depends(get_db),
) -> AuthResponse:
    result = await db.execute(select(User).where(User.username == request.username))
    user = result.scalars().first()
    if user is None or not verify_password(request.password, user.hashed_password):
        raise HTTPException(status_code=status.HTTP_401_UNAUTHORIZED, detail="用户名或密码错误")

    token = create_access_token(user_id=user.id)
    return AuthResponse(access_token=token, user_id=user.id)
```

- [ ] **Step 5: 注册路由到 main.py**

修改 `backend/app/main.py`，添加 auth_router 导入和注册：

```python
from app.api.auth import router as auth_router
from app.api.chat import router as chat_router
from app.api.profile import router as profile_router
from app.api.wiki import router as wiki_router
```

在 `app.include_router(chat_router)` 之前添加：

```python
app.include_router(auth_router)
app.include_router(chat_router)
app.include_router(profile_router)
app.include_router(wiki_router)
```

- [ ] **Step 6: 运行测试确认通过**

Run: `cd backend && uv run pytest tests/test_auth.py -v`
Expected: 9 passed

- [ ] **Step 7: 提交**

```bash
git add backend/app/schemas/auth.py backend/app/api/auth.py backend/app/main.py backend/tests/test_auth.py
git commit -m "feat: 实现注册/登录 API 端点"
```

---

### Task 5: ChatSession 数据隔离 + 保护 chat 端点

**Files:**
- Modify: `backend/app/models/chat.py:6-7,15-31`
- Modify: `backend/app/services/chat_service.py:1-7,17-23,50-54,56-60`
- Modify: `backend/app/api/chat.py:58-63,65-117,120-132,135-175`
- Test: `backend/tests/test_auth.py`（追加）

- [ ] **Step 1: 编写受保护端点测试**

在 `backend/tests/test_auth.py` 末尾追加：

```python
@pytest.mark.asyncio
async def test_chat_sessions_requires_auth() -> None:
    transport = ASGITransport(app=app)
    async with AsyncClient(transport=transport, base_url="http://test") as client:
        resp = await client.get("/api/chat/sessions")
    assert resp.status_code == 403


@pytest.mark.asyncio
async def test_chat_sessions_with_auth() -> None:
    transport = ASGITransport(app=app)
    async with AsyncClient(transport=transport, base_url="http://test") as client:
        reg = await client.post("/api/auth/register", json={
            "username": "chatuser",
            "password": "password123",
        })
        token = reg.json()["access_token"]
        resp = await client.get(
            "/api/chat/sessions",
            headers={"Authorization": f"Bearer {token}"},
        )
    assert resp.status_code == 200
    assert isinstance(resp.json(), list)


@pytest.mark.asyncio
async def test_chat_session_data_isolation() -> None:
    transport = ASGITransport(app=app)
    async with AsyncClient(transport=transport, base_url="http://test") as client:
        reg1 = await client.post("/api/auth/register", json={
            "username": "user_a",
            "password": "password123",
        })
        token_a = reg1.json()["access_token"]

        reg2 = await client.post("/api/auth/register", json={
            "username": "user_b",
            "password": "password123",
        })
        token_b = reg2.json()["access_token"]

        # user_a 创建会话
        resp_a = await client.post(
            "/api/chat/session",
            headers={"Authorization": f"Bearer {token_a}"},
        )
        assert resp_a.status_code == 200

        # user_b 看不到 user_a 的会话
        resp_b = await client.get(
            "/api/chat/sessions",
            headers={"Authorization": f"Bearer {token_b}"},
        )
        assert resp_b.status_code == 200
        assert len(resp_b.json()) == 0
```

- [ ] **Step 2: 运行测试确认失败**

Run: `cd backend && uv run pytest tests/test_auth.py::test_chat_sessions_requires_auth -v`
Expected: FAIL — 200（当前无认证保护）

- [ ] **Step 3: ChatSession 模型添加 user_id 列**

修改 `backend/app/models/chat.py`，在 `ChatSession` 类中添加 `user_id`：

```python
from sqlalchemy import DateTime, ForeignKey, Integer, String, Text
```

在 `ChatSession` 类的 `id` 字段之后添加：

```python
class ChatSession(Base):
    """聊天会话模型。"""

    __tablename__ = "chat_sessions"

    id: Mapped[int] = mapped_column(primary_key=True)
    user_id: Mapped[int | None] = mapped_column(
        Integer, ForeignKey("users.id"), nullable=True
    )
    title: Mapped[str] = mapped_column(String(255), nullable=False)
    created_at: Mapped[datetime] = mapped_column(
        DateTime, default=lambda: datetime.now(UTC), nullable=False
    )
    updated_at: Mapped[datetime] = mapped_column(
        DateTime,
        default=lambda: datetime.now(UTC),
        onupdate=lambda: datetime.now(UTC),
        nullable=False,
    )

    messages: Mapped[list["ChatMessage"]] = relationship(
        back_populates="session",
        cascade="all, delete-orphan",
        order_by="ChatMessage.id",
    )
    resources: Mapped[list["GeneratedResource"]] = relationship(
        back_populates="session",
        cascade="all, delete-orphan",
        order_by="GeneratedResource.id",
    )
```

- [ ] **Step 4: ChatService 添加 user_id 过滤**

修改 `backend/app/services/chat_service.py`：

`create_session` 方法添加 `user_id` 参数：

```python
async def create_session(self, title: str = "新学习会话", user_id: int | None = None) -> int:
    chat_session = ChatSession(title=title, user_id=user_id)
    self._require_session().add(chat_session)
    await self._require_session().commit()
    await self._require_session().refresh(chat_session)
    return chat_session.id
```

`list_sessions` 方法添加 `user_id` 过滤：

```python
async def list_sessions(self, user_id: int | None = None) -> list[ChatSession]:
    stmt = select(ChatSession).order_by(ChatSession.updated_at.desc())
    if user_id is not None:
        stmt = stmt.where(ChatSession.user_id == user_id)
    result = await self._require_session().execute(stmt)
    return list(result.scalars().all())
```

`get_session` 方法添加 `user_id` 过滤：

```python
async def get_session(self, session_id: int, user_id: int | None = None) -> ChatSession | None:
    stmt = select(ChatSession).where(ChatSession.id == session_id)
    if user_id is not None:
        stmt = stmt.where(ChatSession.user_id == user_id)
    result = await self._require_session().execute(stmt)
    return result.scalars().first()
```

`session_exists` 方法添加 `user_id` 过滤：

```python
async def session_exists(self, session_id: int, user_id: int | None = None) -> bool:
    if self.session is None:
        return True
    stmt = select(ChatSession).where(ChatSession.id == session_id)
    if user_id is not None:
        stmt = stmt.where(ChatSession.user_id == user_id)
    result = await self._require_session().execute(stmt)
    return result.scalars().first() is not None
```

- [ ] **Step 5: 保护 chat API 端点**

修改 `backend/app/api/chat.py`：

添加导入：

```python
from app.core.auth import get_current_user
from app.models.user import User
```

修改 `create_chat_session`：

```python
@router.post("/session")
async def create_chat_session(
    db: AsyncSession = Depends(get_db),
    user: User = Depends(get_current_user),
) -> dict[str, int]:
    chat_service = ChatService(session=db)
    session_id = await chat_service.create_session(user_id=user.id)
    return {"session_id": session_id}
```

修改 `list_sessions`：

```python
@router.get("/sessions", response_model=list[ChatSessionResponse])
async def list_sessions(
    db: AsyncSession = Depends(get_db),
    user: User = Depends(get_current_user),
) -> list[ChatSessionResponse]:
    chat_service = ChatService(session=db)
    sessions = await chat_service.list_sessions(user_id=user.id)
    return [
        ChatSessionResponse(
            id=s.id,
            title=s.title,
            created_at=s.created_at.isoformat(),
            updated_at=s.updated_at.isoformat(),
        )
        for s in sessions
    ]
```

修改 `get_session_detail`：

```python
@router.get("/sessions/{session_id}", response_model=SessionDetailResponse)
async def get_session_detail(
    session_id: int,
    db: AsyncSession = Depends(get_db),
    user: User = Depends(get_current_user),
) -> SessionDetailResponse:
    chat_service = ChatService(session=db)
    session = await chat_service.get_session(session_id, user_id=user.id)
    if session is None:
        raise HTTPException(status_code=404, detail="会话不存在")
    # ... 其余不变
```

修改 `stream_chat` — 由于 SSE 生成器不能用 Depends，需要从请求体传 token 或在 ChatRequest 中添加 token 字段。更好的方案是在 `stream_chat` 函数签名中用 Depends 验证用户，然后把 user_id 传入生成器：

```python
@router.post("/stream")
async def stream_chat(
    request: ChatRequest,
    user: User = Depends(get_current_user),
) -> StreamingResponse:
    user_id = user.id

    async def event_stream() -> AsyncGenerator[str, None]:
        async with AsyncSessionLocal() as db:
            chat_service = ChatService(session=db)
            session_id = request.session_id
            if session_id is None:
                session_id = await chat_service.create_session(user_id=user_id)
            elif not await chat_service.session_exists(session_id, user_id=user_id):
                yield encode_sse_event(
                    error_event(
                        message="会话不存在，请先创建会话后再发送消息",
                        session_id=session_id,
                    )
                )
                return

            try:
                await chat_service.save_message(
                    session_id=session_id,
                    role="user",
                    content=request.message,
                )
            except Exception:
                yield encode_sse_event(
                    error_event(
                        message="保存聊天消息失败，请稍后重试",
                        session_id=session_id,
                    )
                )
                return

            orchestrator = build_orchestrator(db)
            try:
                async for event in orchestrator.run(
                    session_id=session_id,
                    user_message=request.message,
                ):
                    yield encode_sse_event(event)
            except LLMClientError as exc:
                yield encode_sse_event(
                    error_event(message=str(exc), session_id=session_id)
                )
            except Exception:
                yield encode_sse_event(
                    error_event(
                        message="聊天处理失败，请稍后重试",
                        session_id=session_id,
                    )
                )

    return StreamingResponse(event_stream(), media_type="text/event-stream")
```

- [ ] **Step 6: 运行测试确认通过**

Run: `cd backend && uv run pytest tests/test_auth.py -v`
Expected: 12 passed

- [ ] **Step 7: 生成 Alembic 迁移**

Run: `cd backend && uv run alembic revision --autogenerate -m "chat_sessions 添加 user_id"`
Expected: 生成迁移文件

- [ ] **Step 8: 应用迁移**

Run: `cd backend && uv run alembic upgrade head`
Expected: 迁移成功

- [ ] **Step 9: 提交**

```bash
git add backend/app/models/chat.py backend/app/services/chat_service.py backend/app/api/chat.py backend/tests/test_auth.py backend/alembic/versions/
git commit -m "feat: chat 端点添加认证保护和数据隔离"
```

---

### Task 6: 保护 profile 和 wiki 端点

**Files:**
- Modify: `backend/app/api/profile.py`
- Modify: `backend/app/api/wiki.py:71-86`
- Modify: `backend/app/services/profile_service.py:35,47-51,89-94`
- Test: `backend/tests/test_auth.py`（追加）

- [ ] **Step 1: 编写 profile 认证测试**

在 `backend/tests/test_auth.py` 末尾追加：

```python
@pytest.mark.asyncio
async def test_profile_requires_auth() -> None:
    transport = ASGITransport(app=app)
    async with AsyncClient(transport=transport, base_url="http://test") as client:
        resp = await client.get("/api/profile")
    assert resp.status_code == 403


@pytest.mark.asyncio
async def test_profile_with_auth() -> None:
    transport = ASGITransport(app=app)
    async with AsyncClient(transport=transport, base_url="http://test") as client:
        reg = await client.post("/api/auth/register", json={
            "username": "profileuser",
            "password": "password123",
        })
        token = reg.json()["access_token"]
        resp = await client.get(
            "/api/profile",
            headers={"Authorization": f"Bearer {token}"},
        )
    assert resp.status_code == 200
    data = resp.json()
    assert data["user_id"] > 0
```

- [ ] **Step 2: 运行测试确认失败**

Run: `cd backend && uv run pytest tests/test_auth.py::test_profile_requires_auth -v`
Expected: FAIL — 200（当前无认证保护）

- [ ] **Step 3: 修改 profile 端点**

修改 `backend/app/api/profile.py`：

```python
from __future__ import annotations

from fastapi import APIRouter, Depends, Query
from sqlalchemy.ext.asyncio import AsyncSession

from app.core.auth import get_current_user
from app.core.database import get_db
from app.models.user import User
from app.schemas.profile import ProfileResponse
from app.services.chat_service import ChatService
from app.services.profile_service import ProfileService

router = APIRouter(prefix="/api/profile", tags=["profile"])


@router.get("", response_model=ProfileResponse)
async def get_profile(
    session_id: int | None = Query(default=None),
    db: AsyncSession = Depends(get_db),
    user: User = Depends(get_current_user),
) -> ProfileResponse:
    chat_service = ChatService(session=db)
    profile_service = ProfileService(session=db)

    current_session_id = session_id
    if current_session_id is None:
        current_session_id = await chat_service.create_session(user_id=user.id)

    return await profile_service.get_or_create_profile(
        session_id=current_session_id,
        user_id=user.id,
    )
```

- [ ] **Step 4: 移除 DEFAULT_USER_ID**

修改 `backend/app/services/profile_service.py`：

删除 `DEFAULT_USER_ID = 1` 常量。

修改 `get_or_create_profile` 签名，移除默认值：

```python
async def get_or_create_profile(
    self,
    session_id: int | None = None,
    user_id: int = 1,
) -> ProfileResponse:
```

修改 `save_profile_update` 签名，移除默认值：

```python
async def save_profile_update(
    self,
    session_id: int,
    update: dict[str, Any],
    user_id: int = 1,
) -> ProfileResponse:
```

注意：保留 `user_id` 参数默认值为 `1`，因为 orchestrator 内部调用时暂时还没有 user 上下文。后续 Task 中 orchestrator 会传入正确的 user_id。

- [ ] **Step 5: 保护 wiki write-back 端点**

修改 `backend/app/api/wiki.py`，仅 `write_back` 端点需要认证（搜索端点保持公开）：

```python
from app.core.auth import get_current_user
from app.models.user import User
```

```python
@router.post("/write-back", response_model=WriteBackResponse)
async def write_back(
    request: WriteBackRequest,
    db: AsyncSession = Depends(get_db),
    user: User = Depends(get_current_user),
) -> WriteBackResponse:
    """Agent 生成内容回写知识库。"""
    wiki = get_wiki_service(session=db)
    chunk_id = await wiki.write_back(
        title=request.title,
        content=request.content,
        source_agent=request.source_agent,
        chapter=request.chapter,
        section=request.section,
        tags=request.tags,
    )
    return WriteBackResponse(success=chunk_id is not None, chunk_id=chunk_id)
```

- [ ] **Step 6: 运行测试确认通过**

Run: `cd backend && uv run pytest tests/test_auth.py -v`
Expected: 14 passed

- [ ] **Step 7: 提交**

```bash
git add backend/app/api/profile.py backend/app/api/wiki.py backend/app/services/profile_service.py backend/tests/test_auth.py
git commit -m "feat: profile 和 wiki write-back 端点添加认证保护"
```

---

### Task 7: 前端 Token 管理 + 请求拦截

**Files:**
- Create: `frontend/src/lib/auth.ts`
- Modify: `frontend/src/lib/api.ts`
- Modify: `frontend/src/lib/sse.ts:57-62`

- [ ] **Step 1: 创建 auth.ts Token 管理**

创建 `frontend/src/lib/auth.ts`：

```typescript
const TOKEN_KEY = "eduagent_token";
const USER_ID_KEY = "eduagent_user_id";

export function saveToken(token: string, userId: number): void {
  localStorage.setItem(TOKEN_KEY, token);
  localStorage.setItem(USER_ID_KEY, String(userId));
}

export function getToken(): string | null {
  if (typeof window === "undefined") return null;
  return localStorage.getItem(TOKEN_KEY);
}

export function getUserId(): number | null {
  if (typeof window === "undefined") return null;
  const id = localStorage.getItem(USER_ID_KEY);
  return id ? Number(id) : null;
}

export function removeToken(): void {
  localStorage.removeItem(TOKEN_KEY);
  localStorage.removeItem(USER_ID_KEY);
}

export function isAuthenticated(): boolean {
  const token = getToken();
  if (!token) return false;
  try {
    const payload = JSON.parse(atob(token.split(".")[1]));
    return payload.exp * 1000 > Date.now();
  } catch {
    return false;
  }
}
```

- [ ] **Step 2: 修改 api.ts 添加 Authorization header**

修改 `frontend/src/lib/api.ts`，添加认证 header 辅助函数和登录/注册 API：

```typescript
import type { Profile, ChatSession, SessionDetail } from "./types";
import { getToken, saveToken } from "./auth";

const API_BASE = process.env.NEXT_PUBLIC_API_URL || "http://localhost:8000";

function authHeaders(): Record<string, string> {
  const token = getToken();
  if (token) {
    return { Authorization: `Bearer ${token}` };
  }
  return {};
}

async function parseJSON<T>(response: Response): Promise<T> {
  try {
    return await response.json();
  } catch {
    throw new Error("服务器返回了无效的响应格式");
  }
}

export async function register(
  username: string,
  password: string,
  displayName?: string
): Promise<{ access_token: string; user_id: number }> {
  const response = await fetch(`${API_BASE}/api/auth/register`, {
    method: "POST",
    headers: { "Content-Type": "application/json" },
    body: JSON.stringify({ username, password, display_name: displayName }),
  });
  if (!response.ok) {
    const data = await response.json().catch(() => ({}));
    throw new Error(data.detail || "注册失败");
  }
  const data = await parseJSON<{ access_token: string; user_id: number }>(response);
  saveToken(data.access_token, data.user_id);
  return data;
}

export async function login(
  username: string,
  password: string
): Promise<{ access_token: string; user_id: number }> {
  const response = await fetch(`${API_BASE}/api/auth/login`, {
    method: "POST",
    headers: { "Content-Type": "application/json" },
    body: JSON.stringify({ username, password }),
  });
  if (!response.ok) {
    const data = await response.json().catch(() => ({}));
    throw new Error(data.detail || "登录失败");
  }
  const data = await parseJSON<{ access_token: string; user_id: number }>(response);
  saveToken(data.access_token, data.user_id);
  return data;
}

export async function createSession(): Promise<number> {
  const response = await fetch(`${API_BASE}/api/chat/session`, {
    method: "POST",
    headers: { ...authHeaders() },
  });
  if (!response.ok) {
    throw new Error("创建会话失败");
  }
  const data = await parseJSON<{ session_id: number }>(response);
  return data.session_id;
}

export async function fetchSessions(): Promise<ChatSession[]> {
  const response = await fetch(`${API_BASE}/api/chat/sessions`, {
    headers: { ...authHeaders() },
  });
  if (!response.ok) {
    throw new Error("获取会话列表失败");
  }
  return parseJSON<ChatSession[]>(response);
}

export async function fetchSessionDetail(
  sessionId: number
): Promise<SessionDetail> {
  const response = await fetch(`${API_BASE}/api/chat/sessions/${sessionId}`, {
    headers: { ...authHeaders() },
  });
  if (!response.ok) {
    throw new Error("获取会话详情失败");
  }
  return parseJSON<SessionDetail>(response);
}

export async function fetchProfile(
  sessionId?: number
): Promise<Profile> {
  const url = sessionId
    ? `${API_BASE}/api/profile?session_id=${sessionId}`
    : `${API_BASE}/api/profile`;
  const response = await fetch(url, {
    headers: { ...authHeaders() },
  });
  if (!response.ok) {
    throw new Error("获取画像失败");
  }
  return parseJSON<Profile>(response);
}

export function getChatStreamUrl(): string {
  return `${API_BASE}/api/chat/stream`;
}
```

- [ ] **Step 3: 修改 sse.ts 添加 Authorization header**

修改 `frontend/src/lib/sse.ts` 中 `streamChat` 函数的 fetch 调用，添加认证 header：

```typescript
import { getToken } from "./auth";
```

修改 fetch 调用部分：

```typescript
const response = await fetch(getChatStreamUrl(), {
  method: "POST",
  headers: {
    "Content-Type": "application/json",
    ...(getToken() ? { Authorization: `Bearer ${getToken()}` } : {}),
  },
  body: JSON.stringify(body),
  signal,
});
```

- [ ] **Step 4: 验证 TypeScript 编译**

Run: `cd frontend && pnpm type-check`
Expected: 无类型错误

- [ ] **Step 5: 提交**

```bash
git add frontend/src/lib/auth.ts frontend/src/lib/api.ts frontend/src/lib/sse.ts
git commit -m "feat: 前端 Token 管理和请求认证拦截"
```

---

### Task 8: 登录/注册页面

**Files:**
- Create: `frontend/src/app/login/page.tsx`

- [ ] **Step 1: 创建登录/注册页面**

创建 `frontend/src/app/login/page.tsx`：

```tsx
"use client";

import { useState } from "react";
import { useRouter } from "next/navigation";
import { login, register } from "@/lib/api";

export default function LoginPage() {
  const router = useRouter();
  const [isRegister, setIsRegister] = useState(false);
  const [username, setUsername] = useState("");
  const [password, setPassword] = useState("");
  const [displayName, setDisplayName] = useState("");
  const [error, setError] = useState("");
  const [loading, setLoading] = useState(false);

  const handleSubmit = async (e: React.FormEvent) => {
    e.preventDefault();
    setError("");
    setLoading(true);

    try {
      if (isRegister) {
        await register(username, password, displayName || undefined);
      } else {
        await login(username, password);
      }
      router.push("/");
    } catch (err) {
      setError(err instanceof Error ? err.message : "操作失败，请重试");
    } finally {
      setLoading(false);
    }
  };

  return (
    <div className="flex min-h-screen items-center justify-center bg-[var(--color-parchment)]">
      <div className="w-full max-w-sm rounded-xl bg-[var(--color-ivory)] p-8 ring-1 ring-[var(--color-warm-gray-200)]">
        <h1 className="mb-6 text-center font-serif text-2xl font-medium text-[var(--color-terracotta)]">
          EduAgent
        </h1>
        <h2 className="mb-4 text-center text-sm text-[var(--color-warm-gray-600)]">
          {isRegister ? "创建账号" : "登录"}
        </h2>

        <form onSubmit={handleSubmit} className="space-y-4">
          <div>
            <label className="mb-1 block text-xs text-[var(--color-warm-gray-500)]">
              用户名
            </label>
            <input
              type="text"
              value={username}
              onChange={(e) => setUsername(e.target.value)}
              required
              minLength={2}
              maxLength={50}
              className="w-full rounded-lg border border-[var(--color-warm-gray-200)] bg-white px-3 py-2 text-sm outline-none transition-colors focus:border-[var(--color-terracotta)] focus:ring-1 focus:ring-[var(--color-terracotta)]"
              placeholder="请输入用户名"
            />
          </div>

          <div>
            <label className="mb-1 block text-xs text-[var(--color-warm-gray-500)]">
              密码
            </label>
            <input
              type="password"
              value={password}
              onChange={(e) => setPassword(e.target.value)}
              required
              minLength={6}
              maxLength={128}
              className="w-full rounded-lg border border-[var(--color-warm-gray-200)] bg-white px-3 py-2 text-sm outline-none transition-colors focus:border-[var(--color-terracotta)] focus:ring-1 focus:ring-[var(--color-terracotta)]"
              placeholder="请输入密码"
            />
          </div>

          {isRegister && (
            <div>
              <label className="mb-1 block text-xs text-[var(--color-warm-gray-500)]">
                显示名称（选填）
              </label>
              <input
                type="text"
                value={displayName}
                onChange={(e) => setDisplayName(e.target.value)}
                maxLength={100}
                className="w-full rounded-lg border border-[var(--color-warm-gray-200)] bg-white px-3 py-2 text-sm outline-none transition-colors focus:border-[var(--color-terracotta)] focus:ring-1 focus:ring-[var(--color-terracotta)]"
                placeholder="请输入显示名称"
              />
            </div>
          )}

          {error && (
            <p className="rounded-lg bg-[#fef2f0] px-3 py-2 text-xs text-[#c96442]">
              {error}
            </p>
          )}

          <button
            type="submit"
            disabled={loading}
            className="w-full rounded-lg bg-[var(--color-terracotta)] px-4 py-2 text-sm font-medium text-white transition-opacity hover:opacity-90 disabled:opacity-50"
          >
            {loading ? "处理中..." : isRegister ? "注册" : "登录"}
          </button>
        </form>

        <p className="mt-4 text-center text-xs text-[var(--color-warm-gray-500)]">
          {isRegister ? "已有账号？" : "没有账号？"}
          <button
            type="button"
            onClick={() => {
              setIsRegister(!isRegister);
              setError("");
            }}
            className="ml-1 text-[var(--color-terracotta)] hover:underline"
          >
            {isRegister ? "去登录" : "去注册"}
          </button>
        </p>
      </div>
    </div>
  );
}
```

- [ ] **Step 2: 验证 TypeScript 编译**

Run: `cd frontend && pnpm type-check`
Expected: 无类型错误

- [ ] **Step 3: 提交**

```bash
git add frontend/src/app/login/page.tsx
git commit -m "feat: 添加登录/注册页面"
```

---

### Task 9: 路由保护 + 首页重定向

**Files:**
- Modify: `frontend/src/app/(main)/layout.tsx`
- Modify: `frontend/src/app/page.tsx`

- [ ] **Step 1: 修改 (main)/layout.tsx 添加路由保护**

将 `frontend/src/app/(main)/layout.tsx` 改为客户端组件，添加认证检查：

```tsx
"use client";

import { useEffect, useState } from "react";
import { useRouter } from "next/navigation";
import Link from "next/link";
import { isAuthenticated, removeToken } from "@/lib/auth";
import { SessionSidebar } from "@/components/chat/SessionSidebar";

export default function MainLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  const router = useRouter();
  const [checked, setChecked] = useState(false);

  useEffect(() => {
    if (!isAuthenticated()) {
      router.replace("/login");
    } else {
      setChecked(true);
    }
  }, [router]);

  const handleLogout = () => {
    removeToken();
    router.replace("/login");
  };

  if (!checked) {
    return (
      <div className="flex min-h-screen items-center justify-center bg-[var(--color-parchment)]">
        <p className="text-[var(--color-warm-gray-500)]">加载中...</p>
      </div>
    );
  }

  return (
    <div className="flex min-h-screen flex-col bg-[var(--color-parchment)]">
      <header className="border-b border-[var(--color-warm-gray-200)] bg-[var(--color-ivory)]/95 backdrop-blur-sm">
        <div className="flex max-w-full items-center justify-between px-4 py-3">
          <Link
            href="/"
            className="text-xl font-medium text-[var(--color-terracotta)] font-serif"
          >
            EduAgent
          </Link>
          <div className="flex items-center gap-4">
            <nav className="flex gap-4 text-sm">
              <Link
                href="/"
                className="text-[var(--color-warm-gray-600)] transition-colors hover:text-[var(--color-terracotta)]"
              >
                对话
              </Link>
              <Link
                href="/profile"
                className="text-[var(--color-warm-gray-600)] transition-colors hover:text-[var(--color-terracotta)]"
              >
                学习画像
              </Link>
            </nav>
            <button
              onClick={handleLogout}
              className="text-sm text-[var(--color-warm-gray-500)] transition-colors hover:text-[var(--color-terracotta)]"
            >
              退出
            </button>
          </div>
        </div>
      </header>
      <div className="flex flex-1 overflow-hidden">
        <SessionSidebar />
        <main className="flex-1 overflow-hidden bg-[var(--color-parchment)]">
          <div className="h-full p-3 md:p-4">{children}</div>
        </main>
      </div>
    </div>
  );
}
```

- [ ] **Step 2: 修改首页添加认证检查**

修改 `frontend/src/app/page.tsx`：

```tsx
"use client";

import { useEffect } from "react";
import { useRouter } from "next/navigation";
import { isAuthenticated } from "@/lib/auth";
import { fetchSessions, createSession } from "@/lib/api";

export default function HomePage() {
  const router = useRouter();

  useEffect(() => {
    if (!isAuthenticated()) {
      router.replace("/login");
      return;
    }

    fetchSessions()
      .then((sessions) => {
        if (sessions.length > 0) {
          router.replace(`/chat/${sessions[0].id}`);
        } else {
          return createSession().then((sessionId) => {
            router.replace(`/chat/${sessionId}`);
          });
        }
      })
      .catch(() => {
        createSession()
          .then((sessionId) => {
            router.replace(`/chat/${sessionId}`);
          })
          .catch(() => {
            router.replace("/login");
          });
      });
  }, [router]);

  return (
    <div className="flex min-h-screen items-center justify-center bg-[var(--color-parchment)]">
      <div className="text-center">
        <h1 className="text-3xl text-[var(--color-terracotta)] mb-4 font-serif">
          EduAgent
        </h1>
        <p className="text-[var(--color-warm-gray-500)]">正在准备学习环境...</p>
      </div>
    </div>
  );
}
```

- [ ] **Step 3: 验证 TypeScript 编译**

Run: `cd frontend && pnpm type-check`
Expected: 无类型错误

- [ ] **Step 4: 提交**

```bash
git add frontend/src/app/\(main\)/layout.tsx frontend/src/app/page.tsx
git commit -m "feat: 前端路由保护和登出功能"
```

---

### Task 10: 端到端验证

**Files:** 无新文件

- [ ] **Step 1: 运行全部后端测试**

Run: `cd backend && uv run pytest -v`
Expected: 全部通过

- [ ] **Step 2: 运行前端类型检查**

Run: `cd frontend && pnpm type-check`
Expected: 无类型错误

- [ ] **Step 3: 运行前端 lint**

Run: `cd frontend && pnpm lint`
Expected: 无错误

- [ ] **Step 4: 验证后端启动**

Run: `cd backend && timeout 5 uv run uvicorn app.main:app --port 8000 || true`
Expected: 启动无报错（5 秒后自动退出）

- [ ] **Step 5: 手动验证清单**

启动前后端后，手动验证以下流程：

1. 访问 `http://localhost:3000` → 重定向到 `/login`
2. 注册新用户 → 成功后跳转到对话页
3. 刷新页面 → 保持登录状态
4. 点击"退出" → 跳转到 `/login`
5. 用刚注册的账号登录 → 成功
6. 发送聊天消息 → SSE 流式响应正常
7. 用另一个账号注册 → 看不到第一个用户的会话
8. 访问 `/api/chat/sessions`（无 token）→ 返回 403
9. 访问 `/health` → 返回 200（无需认证）

- [ ] **Step 6: 最终提交（如有修复）**

```bash
git add -A
git commit -m "fix: 端到端验证修复"
```
