# 05 · AI 工具与开源合规说明（金漪湖 · 智能体融合创新赛道版）

> 作品在 MCP 引擎侧沿用 DeepSeek + OpenAI 兼容双模型，remio 侧使用平台自身能力。
> 详见 `remio/docs/05_AI工具与开源合规说明.md`。本文件是工程文档口径，内容与提交口径
> 保持一致，要点如下。

## 1. AI 工具选型

| 用途 | 工具 | 口径 |
| --- | --- | --- |
| 多 Agent 生成/路由/答疑 | DeepSeek（主）+ OpenAI 兼容（备） | MCP 引擎侧 |
| 兼容 LLM | 阿里云百炼 qwen | 可选 |
| 语音识别/图片 | 百炼 DashScope | 可选 |
| 内容安全 | 本地过滤规则 | 不安全片段过滤 + 免责声明 |
| 智能体平台 | remio 睿妙 | 赛道指定，承载 aApp/skill |
| 跨产品调用 | MCP（自研） | 标准库实现，零新增依赖 |
| 视频搜索 | Tavily（限 B 站） | 可选 |

## 2. 开源依赖与协议（主要）

后端：FastAPI(MIT)、Uvicorn/Starlette(BSD-3)、SQLAlchemy(MIT)、Pydantic(MIT)、
httpx(BSD-3)、tenacity(Apache-2.0)、LangGraph/LangChain(MIT)、ChromaDB(Apache-2.0)、
sentence-transformers(Apache-2.0)、PyTorch(BSD-style)、scikit-learn/SciPy/NumPy(BSD-3)。

前端：Next.js(MIT)、React(MIT)、Tailwind CSS(MIT)、shadcn/ui(MIT)、recharts(MIT)、zustand(MIT)。

模型：BAAI/bge-small-zh-v1.5(MIT)；《计算机网络》知识库为自建。

## 3. 合规声明

- 遵守各组件许可证：保留版权、MIT/BSD 随分发附许可证、Apache-2.0 注明修改并保留 NOTICE。
- 未篡改/去版权标识；依赖经 uv/pnpm 锁定版本；自研代码原创。
- LLM 生成内容经 RAG 锚定 + 来源引用，不构成第三方内容复制分发。

## 4. 提交前必填

- [ ] 确认最终启用的模型组合，删除未启用项。
- [ ] 在 `remio/aapp/eduagent-aapp-manifest.json` 的 `developer` 补团队信息。
- [ ] 发布市场前按 remio 要求补完整性签名与实名。